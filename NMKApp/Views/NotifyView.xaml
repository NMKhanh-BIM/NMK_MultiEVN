<UserControl x:Class="NMKApp.Views.NotifyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:NMKApp.Converters">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolVisConv"/>
        <converters:DateFormatConverter x:Key="DateConv"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="White" Padding="16,12">
            <Border.Effect>
                <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.08"/>
            </Border.Effect>
            <DockPanel>
                <Button Content="Mark All as Read" Style="{StaticResource PrimaryButtonStyle}"
                        DockPanel.Dock="Right" FontSize="12" Padding="12,6"
                        Command="{Binding MarkAllAsReadCommand}"/>
                <TextBlock Text="Notifications" FontSize="20" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
            </DockPanel>
        </Border>

        <!-- Notifications List -->
        <ScrollViewer Grid.Row="1" Margin="16">
            <ItemsControl ItemsSource="{Binding Notifications}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="8" Margin="0,0,0,8" Padding="16,12"
                                Cursor="Hand">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.08"/>
                            </Border.Effect>
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRead}" Value="False">
                                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                            <Setter Property="BorderThickness" Value="0,0,3,0"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14"
                                               Foreground="{StaticResource TextPrimaryBrush}"/>
                                    <TextBlock Text="{Binding Content}" FontSize="12" Margin="0,4,0,0"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               TextWrapping="Wrap"/>
                                    <TextBlock FontSize="11" Margin="0,6,0,0" Foreground="#9E9E9E">
                                        <Run Text="From: "/>
                                        <Run Text="{Binding ActorEmail, Mode=OneWay}"/>
                                        <Run Text=" • "/>
                                        <Run Text="{Binding CreatedAt, Mode=OneWay, Converter={StaticResource DateConv}}"/>
                                    </TextBlock>
                                </StackPanel>
                                <Button Grid.Column="1" Content="Mark Read" FontSize="11"
                                        Style="{StaticResource PrimaryButtonStyle}" Padding="10,4"
                                        VerticalAlignment="Top"
                                        Command="{Binding DataContext.MarkAsReadCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding Id}">
                                    <Button.Visibility>
                                        <Binding Path="IsRead" Converter="{StaticResource BoolVisConv}" ConverterParameter="Invert"/>
                                    </Button.Visibility>
                                </Button>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
