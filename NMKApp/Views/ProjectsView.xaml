<UserControl x:Class="NMKApp.Views.ProjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Background="White" Padding="16,12">
            <Border.Effect><DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.08"/></Border.Effect>
            <TextBlock Text="Projects" FontSize="20" FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}"/>
        </Border>
        <ScrollViewer Grid.Row="1" Margin="16">
            <ItemsControl ItemsSource="{Binding Projects}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,16,16">
                            <StackPanel>
                                <Border Background="#37474F" Height="140" CornerRadius="8,8,0,0">
                                    <Viewbox Width="80" Height="80">
                                        <Canvas Width="100" Height="100">
                                            <Polygon Points="50,10 90,35 90,65 50,90 10,65 10,35" Fill="#546E7A"/>
                                            <Polygon Points="50,10 90,35 50,55 10,35" Fill="#E53935"/>
                                            <Polygon Points="10,35 50,55 50,90 10,65" Fill="#BDBDBD"/>
                                            <Polygon Points="90,35 50,55 50,90 90,65" Fill="#9E9E9E"/>
                                        </Canvas>
                                    </Viewbox>
                                </Border>
                                <Border Background="#F57C00" Padding="12,10" CornerRadius="0,0,8,8">
                                    <StackPanel>
                                        <TextBlock Foreground="White" FontSize="12">
                                            <Run Text="Name : " FontWeight="Bold"/><Run Text="{Binding Name, Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock Foreground="White" FontSize="12" Margin="0,2,0,0">
                                            <Run Text="Version : " FontWeight="Bold"/><Run Text="{Binding RevitVersion, Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock Foreground="White" FontSize="12" Margin="0,2,0,0"
                                                   TextTrimming="CharacterEllipsis">
                                            <Run Text="Description : " FontWeight="Bold"/><Run Text="{Binding Description, Mode=OneWay}"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
